# This is the database URL for your PostgreSQL instance.
# MUST BE SECURELY configured in your deployment environment, NOT in the client/Tauri app.
# This template assumes a separate backend service handles database access (as required for SaaS).

DATABASE_URL="postgresql://user:password@host:port/dbname?schema=public"

# Tauri/Vite environment variables
VITE_API_URL="http://localhost:3000/api/v1"